---
.tag:create:
  variables:
    JOB_CODE_REGEX: /tag:create/
  script:
    - git clone "https://${CONF_CUSERNAME}:${CONF_CTOKEN}@${CI_SERVER_HOST}/${CI_PROJECT_PATH}.git" /tmp/workspace
    - create_tag.py /tmp/workspace
